## Удобнее в VS Code

1. Diff в один клик — сразу видно, что изменилось
2. Кнопки commit/push/pull в UI 
3. GitLens показывает автора каждой строки

## Удобнее в терминале

- Interactive rebase (`git rebase -i`) — в VS Code его нет, а через терминал можно за секунду переписать историю